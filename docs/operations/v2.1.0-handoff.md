# v2.1.0 交接与待办

## 当前状态

- 分支 `feat/v2.1.0-prompt-write-path` 已并入 `main` 并推送（2026-09-22）。
- 线上两个迁移已应用并逐条核对通过，依据见 [数据库迁移](database-migrations.md)。
- 功能面已完成：本地提示词写入统一资产、云端数据源切到统一资产与云端函数、备份往返、
  切换演练、回滚实测。
- 证据：`npm run check` 退出码 0，209 项测试全部通过，生产构建成功。

## 还没做完的事

1. 在能访问 Vercel 的网络里，打开线上域名自测一遍。域名现在从本机能访问，
   但站点外面套着 Vercel 部署保护，需要先用自己的 Vercel 账号登录，再用应用账号进入；
   这一步要产品负责人本人操作，见 [待确认与待办事项](../pending-items.md) 第 2 行。

（`v2.1.0`、`v2.1.1`、`v2.1.2` 三个标签都已在 2026-09-22 打好并推送。）

## 回滚须知（一直有效）

回滚前必须先用 2.1.0 导出一份提示词备份。实测结果表明，直接切回旧代码后旧提示词表里
没有切换之后写入的数据，必须靠备份补回来。垃圾箱里的内容不在备份里。

## 已知边界

1. 云端读写已用真实账号走过界面（见 `docs/acceptance/v2.1.0.md` 的验收记录），但那次跑的是
   本机以云端模式启动的服务，不是 Vercel 线上域名。
2. ~~备份只覆盖提示词~~ 已在 `2.1.2` 修掉：备份升级到 v2，含项目、规则、文档、技术档案和关系。
3. 状态变化（归档、重新激活）也会留下一条版本记录，和 2.0.0 的行为一致。
4. `prompts` 和 `prompt_versions` 表保留为只读的迁移前快照，不清理。
