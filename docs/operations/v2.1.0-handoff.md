# v2.1.0 交接与待办

## 当前状态

- 分支 `feat/v2.1.0-prompt-write-path`（已推送），**尚未合并到 `main`**。
- 功能面已完成：本地提示词写入统一资产、云端数据源切到统一资产与云端函数、备份往返、
  切换演练、回滚实测。
- 证据：`npm run check` 退出码 0，208 项测试全部通过，生产构建成功。

## 需要人工处理的事（按顺序）

### 1. Supabase 迁移（阻塞云端验证和合并）

1. 在 Supabase 迁移历史里确认 `20260921054052_add_project_asset_foundation.sql` 是否
   已经应用。推送到 `main` 有自动应用集成，2026-09-20 用真实迁移验证过，可能已经自动
   执行。
2. 应用 `20260921120000_add_asset_prompt_storage.sql`（2.1.0 新增，7 个统一资产函数）。
3. 在 Supabase 跑一次顾问检查，确认没有 error。

这一步没做完，云端模式跑不起来，2.1.0 也不适合合并——合并会同时触发迁移自动应用和
生产部署，一旦函数有问题，生产会在保存提示词时直接失败。

### 2. 合并 2.1.0（等第 1 步完成）

建议顺序：迁移应用并通过顾问检查 → 合并到 `main` → 在生产上各走一遍新建、编辑、删除、
恢复提示词。

回滚须知：回滚前必须先用 2.1.0 导出一份提示词备份。实测结果表明，直接切回旧代码后
旧提示词表里没有切换之后写入的数据，必须靠备份补回来。垃圾箱里的内容不在备份里。

### 3. 签验收清单

- `docs/acceptance/v2.0.0.md`（15 项手工 + 3 项云端）
- `docs/acceptance/v2.1.0.md`（云端那三项目前无法勾选，依赖第 1 步）

### 4. 2.0.1 设计待确认

见 `docs/superpowers/specs/2026-09-21-v2.0.1-import-and-metadata-design.md` 第六节，
三个问题：云端原文存放方式、ZIP 包大小上限、一次导入最多创建多少资产。这三个都涉及
外部依赖或持续成本，按项目规则需要人工确认后才开工。

## 已知边界

1. 云端只有契约测试覆盖，没有真实运行验证——当前环境没有 Postgres。
2. 备份只覆盖提示词，不含规则和文档；完整备份升级排在 `2.0.2`。
3. 状态变化（归档、重新激活）也会留下一条版本记录，和 2.0.0 的行为一致。
4. `prompts` 和 `prompt_versions` 表保留为只读的迁移前快照，不清理。
