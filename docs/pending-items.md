# 待确认与待办事项

## 用途

给产品负责人和接手会话用。这里只列需要人操作或拍板的事，以及已经定过、不必再问的事。
做完一项就把对应行删掉，不要在这里写过程描述。

更新时间：2026-09-22。

## 需要你操作或拍板

| # | 事项 | 需要做什么 | 完成标准 | 依据 |
| --- | --- | --- | --- | --- |
| 1 | 本地手工验收（两个版本） | 在浏览器点一遍 v2.0.0 和 v2.1.0 的第一到五节；也可以先让我用浏览器自动化代跑，你只看结论 | 两份清单第 1–5 节勾完 | [v2.0.0 验收](acceptance/v2.0.0.md)、[v2.1.0 验收](acceptance/v2.1.0.md) |
| 2 | 云端验收（两个版本） | 用自己的账号登录线上站点，点一遍两份清单第六节 | 第六节勾完 | 同上 |
| 3 | 三个历史分支的处置 | 回复“删”或“留” | 分支数收敛 | 见下方分支现状 |
| 4 | 打 `v2.1.0` 版本标签 | 验收签完后回复“打标签” | 仓库里存在 `v2.1.0` 标签 | 项目规则：每个阶段完成后记录提交和版本标签 |
| 5 | 2.0.1 开工 | 回复“开工” | 开始第 1 个任务（原始文件上传与来源包资产） | [2.0.1 设计](superpowers/specs/2026-09-21-v2.0.1-import-and-metadata-design.md) |

分支现状（2026-09-22 核查）：

- `feat/v0.8.0-ai-prompt-merge`：已并入 `main`，远端同步；但挂着一个旧工作区
  `C:\Users\90402\.codex\worktrees\v080-ai-prompt-merge`，删分支前要先移除工作区。
- `feat/v0.9.0-ai-prompt-optimize`：已并入 `main`，本地多出的那个提交也在 `main` 里，
  删掉不丢代码。
- `feat/rule-asset-center`：**没有**并入 `main`，只有 1 个提交（`123b63b`），属于已作废方向；
  要删就先确认那个提交不需要留档。

## 已经决定，不必再问

- 种子资产包的四个缺口（协作边界、成本与额度、跨团队追踪、监管高敏条款）等 2.0.1 之后
  按需补，遵循“宁精勿多”。
- 后台版本服务支线确认为重复实现，不合并，分支已删除。
- 检查分两档、lint 缓存、证据只记一处、工程地图，四项已生效。
- 种子资产包 `0.2.1` 已确认并并入 main，等 2.0.1 拿它当导入样本。
- 2.0.1 三个待定问题已定：云端原文存 Supabase Storage 私有空间；ZIP 单包上限 20 MB；
  单次导入最多 200 个资产。
- 迁移执行方式：会话里有 Supabase MCP 就用 MCP，没有就用数据库密码走
  `supabase db push --db-url`。

## 正在进行，不属于待确认

- 2.1.0 提示词写入路径切换：已并入 `main` 并推送，线上迁移已应用；剩下签字和打标签。
- 备份范围（规则和文档进备份）：排在 2.0.2。
