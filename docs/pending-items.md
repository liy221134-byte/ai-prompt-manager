# 待确认与待办事项

## 用途

给产品负责人和接手会话用。这里放三类内容：需要人操作或拍板的事、已经定过不必再问的事、
需要留痕的记录（分支处置、规则命中）。待办做完就把对应行删掉；记录保留，不写过程描述。

更新时间：2026-09-24。

## 需要你操作或拍板

| # | 事项 | 需要做什么 | 完成标准 | 依据 |
| --- | --- | --- | --- | --- |
| 1 | 线上域名自测 | 打开 `https://ai-prompt-manager-liyunqi.vercel.app`，先用 Vercel 账号过部署保护，再用应用账号登录点一遍（顺带看一眼项目设置里的「质量等级」） | 线上域名可用 | 域名外面套着 Vercel 部署保护，需要你的 Vercel 账号；应用账号是 `liyunqi233@163.com`。2026-09-24 已做第一轮自测并留下反馈 |
| 2 | v2.12.0 验收 | 按 [v2.12.0 验收清单](acceptance/v2.12.0.md) 走一遍；线上部分按[用户故事](user-stories.md)逐条点 | 清单签完，再决定打标签和推送 | 来自 2026-09-24 线上自测反馈，方案 6 个决策点已确认并落地 |
| 3 | ~~云端一条脏数据怎么处置~~ | **已按「修正」处理（2026-09-24）** | 已核对：全库 0 条不合法规则、0 条不合法版本 | 记录见 `operations/database-migrations.md` 的「事实记录」 |
| 4 | ~~v2.13.0 开发~~ | **已完成（2026-09-24）**，与 v2.12.0 一起上线 | 验收清单见 `acceptance/v2.13.0.md` | 来自第二轮自测 + 本地链路走查 |
| 5 | 线上过一遍 v2.13.0／v2.14.0／v2.15.0 | 线上部署完成后，按三份验收清单的「需要线上确认的」各走一遍 | 清单签完 | v2.12.0～v2.15.0 已全部上线 |
| 6 | 立项前的沉淀收口 | 用「沉淀体检」把项目里该升的规则升到公共库；升完核对公共库版本 | 公共库里的法则级内容是最新的 | 你开新项目前要做的事 |

## 待排期（第二轮自测剩下的问题）

第二节那 10 条里，除了已修的 4 条，其余是「不知道怎么用」和方向性问题，结论如下：

1. **工程基线点「新建文档」应该能关联已有文档**（而不是重贴一遍）——采纳，做法是：
   缺口那一行如果库里已经有同类文档，直接给「打开已有的这份」，没有才进新建。排下一轮。
2. **图谱节点怎么用**——当前可用的链路是：节点资产用「资产关系」把需求/设计/接口/测试
   文档连起来，项目图谱的「指向谁／谁指向它／间接影响」读的就是这批关系；节点也可以从
   SQL 建表语句或代码目录导入。下一轮补：在图谱视图和节点编辑器里写清这条链路，
   并支持从节点直接「关联一份文档资产」。
3. **验收记录要能关联录入**——现在能从工程基线的「验收覆盖」带出对应需求节点；
   下一轮补：新建验收记录时直接挑一条需求节点和已有的验收清单文档。
4. **公共资产与项目资产的自动归类 + 本地 MCP 自动接入**——方向认可，属于一条新链路
   （内容自动分流、MCP 批量入库），等前面几条做完单独立项，走一次设计。

分支处置记录（2026-09-22）：三个历史分支已删除，本地与远端都已清理。

2026-09-24 分支清理（2.0 收口后执行）：按 9-23 的决定统一清理，删除前逐个核对过
**相对 `main` 的未合并提交都是 0**，代码不丢：

| 分支 | 删除前的提交 |
| --- | --- |
| 本地 `feat/v2.1.0-prompt-write-path`（含它挂的工作区） | `f3ab494` |
| 远端 `docs/engineering-seed-pack` | `712d1c2` |
| 远端 `feat/v0.9.1-variable-management` | `d114944` |
| 远端 `feat/week-05-supabase-cloud` | `da60b22` |
| 远端 `feat/week-06-ai-capture` | `2171baf` |
| 远端 `fix/optimize-restore-and-scroll-lock` | `b7370ec` |
| 远端 `fix/week-07-auth-hardening` | `3cadef1` |

清理后：本地只剩 `main`，远端只剩 `origin/main`，工作区只剩项目目录本身。

- `feat/v0.8.0-ai-prompt-merge`（`8798e9c`）和 `feat/v0.9.0-ai-prompt-optimize`（`3f8304a`）
  都已并入 `main`，删除不丢代码；前者挂着的旧工作区已一并移除。
- `feat/rule-asset-center`（`123b63b`）没有并入 `main`，属于已作废方向。这个提交对象在本地
  仓库里还能通过该编号找回，远端已不可见。
- 其余历史分支（`feat/week-05`、`feat/week-06`、`fix/week-07`、`feat/v0.9.1`、
  `docs/engineering-seed-pack`、`fix/optimize-restore-and-scroll-lock`）
  2026-09-22 那次未处理，2026-09-24 已统一清理（见上表）。

## 已经决定，不必再问

- 种子资产包的四个缺口（协作边界、成本与额度、跨团队追踪、监管高敏条款）等 2.1.1 之后
  按需补，遵循“宁精勿多”。
- 后台版本服务支线确认为重复实现，不合并，分支已删除。
- 检查分两档、lint 缓存、证据只记一处、工程地图，四项已生效。
- 种子资产包 `0.2.1` 已确认并并入 main，等 2.1.1 拿它当导入样本。
- 2.1.1 三个待定问题已定：云端原文存 Supabase Storage 私有空间；ZIP 单包上限 20 MB；
  单次导入最多 200 个资产。
- 迁移执行方式：会话里有 Supabase MCP 就用 MCP，没有就用数据库密码走
  `supabase db push --db-url`。
- 引入开发依赖 `@electric-sql/pglite`，让每次改动迁移都能在真实 Postgres 上跑一遍
  （只用于测试，不进生产运行时代码）。
- 新项目模板（`templates/new-project/`）不主动改：我只在要求整理优化资产包时，同步更新模板。
  规则的进出只在主 `AGENTS.md` 维护；模板漂移检查（测试）暂不加。
- 种子资产包和新项目模板库导入产品做验证：**已做**（2026-09-23）。产品侧走「导入规则包」
  和「导入模板」装进库里验证；另一个会话另加了 `scripts/import-seed-content.mjs`，
  按应用同一条写入路径（`save_asset` RPC、RLS 生效）把种子包和 4 个模板装进线上库，
  41 条资产核对通过，脚本可重复执行（已存在的按标识跳过）。

## 正在进行，不属于待确认

- 2.1.0 提示词写入路径切换：已并入 `main` 并推送，线上迁移已应用，两份验收清单已由
  Codex 代跑并勾完（记录见清单顶部的「验收记录」）；剩下打标签。
- 备份范围（规则和文档进备份）：已在 2.1.2 交付（备份升级到 v2，含关系）。
- 2.1.2（技术档案、资产关系、备份升级）：功能、界面补验、验收清单记录都已完成，
  已打标签并推送，2026-09-23 产品负责人签字。
- 加固与验收（计划里写作 2.0.3，清单编号 2.1.3）：**挂起**。本机部分已跑完——真实本地库副本的
  升级演练、导入后版本记录核对、筛选目标失效自动复位的修复与实测，记录见
  [2.1.3 验收清单](acceptance/v2.1.3.md)；按产品负责人 2026-09-23 的决定，
  这一版等**整个 2.0 产品**的功能都做完之后统一做，临时产物清理也跟着挪。
- v2.8.0「质量等级 + 工程基线」已签字并打标签，线上迁移已确认应用。
- v2.10.0「发布门禁与故障演练」已签字并打标签，2.0 功能线全部完成。
- v2.11.0「统一加固与验收」已签字并打标签，2.0 这条线整体收口
  （演练记录见 [v2.11.0 验收清单](acceptance/v2.11.0.md)）。
  演练顺带修了三处：跨版本回滚的报错太含糊、备份说明没写历史版本不进备份、
  手工复制库文件的 WAL 陷阱（后两条写进运维文档）。
- 线上域名自测等产品负责人到电脑前再执行。
- 外部来源采集已开张：superpowers 采到 1 条（`PLAYBOOK-DEBUG-001` 系统化调试，已写进
  AGENTS.md 的四步流程）；操作者训练包 `0.1.0` 试装 9 条（心法、M2 派活、M3 驭程、
  M4 验收、M5 排错、M7 沉淀），用现有规则包机制装进产品验证过（8 条规则 + 1 条模板）。
  资产都还是 `candidate`，等你确认后升 `active`。
- MCP 服务用的是本机 SQLite（和界面同一个库文件），云端 Supabase 库不参与。
- v2.2.0（规则包 + 种子资产包导入）：六步全部完成——数据模型与校验、
  种子包解析与规则包文件生成（`npm run pack:seed`）、规则的理由与来源片段、
  规则包详情与安装到项目（含按包筛选）、导入规则包文件的界面、备份兼容与验收清单。
  顺带修掉两个真 bug：技术档案的编辑入口拿不到资产、种子包附属文档缺包内编号导致安装被跳过。
  需求草案已按 2026-09-23 的决定回填（规则包、理由与来源片段、规则集导出，
  独立规则中心的载体继续判废）。
- v2.4.0「模板中心」（模板资产、导入模板、编译套模板）已签字并打标签。
- 演练和补验用的临时文件都在 `%TEMP%` 下（`repro212b`、`empty212c`、`empty212d`、
  `upgrade-drill`、`backup-*.json` 和几个演练库），等 MVP 收尾统一删除，产品负责人已同意。

## 规则命中记录

规则写了不等于会被执行。同一条规则被违反 2 次以上，才允许改 `AGENTS.md`；改的时候要同时
说明删掉或合并了哪条规则，防止规则越加越厚。单次异常不改规则。

| 日期 | 规则 | 发生了什么 |
| --- | --- | --- |
| 暂无 | | |
