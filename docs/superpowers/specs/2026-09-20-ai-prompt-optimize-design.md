# AI 提示词优化设计

## 文档状态

- 方案：B —— 元数据 + 正文结构优化，只做整理和规范化，不改变原意。
- 版本目标：`v0.9.0`（暂定，实施前确认）。
- 状态：范围已确认。2026-09-21 做过一次范围收敛（砍掉两项、补充四项），见文末「已定方案」。
- 前置依赖：`v0.8.0`（AI 合并与垃圾箱）先合并到 `main`，提供 `prompt_versions`
  快照表、`prompts` 的软删除字段和清理任务。

## 目标

让一条已有提示词在**不改变原意**的前提下变得更好用：元数据更准确、正文结构更
统一、变量更规范；优化结果必须由用户预览确认，并且可以随时回到优化前。

核心流程：

```text
打开提示词
-> 点击「AI 优化」
-> AI 返回优化草稿和优化说明
-> 用户对比、编辑、确认
-> 保存并生成优化前快照
-> 需要时可以回到优化前
```

## 本期范围

### 包含

- 元数据优化：标题、分类、标签、适用场景。
- 正文结构优化：只做整理和规范化，不改变原意。
- 可选「优化要求」输入，用于说明整理方向。
- 优化说明：AI 逐条说明改了什么、没改什么。
- 优化前后差异对比：按行显示新增、删除和修改。
- 变量变化清单：写法统一的前后对照。
- 保存前可编辑全部字段。
- 保存时自动生成「优化前」快照，支持一键回到优化前（只回退最近一次优化）。
- 本地 SQLite 与云端 Supabase 行为一致。

### 不包含

- 按目标改写（更简洁版、更严格版、面向特定模型的版本），属于方案 C。
- 批量优化多条提示词。
- 把合并记录和优化记录合并成统一的完整版本历史。
- 提示词分支和合并关系图。
- 手动创建版本、手动命名版本。

## 优化规则

这一节是本方案的核心：明确 AI 允许改什么、禁止改什么。

### 允许的改动（白名单）

1. 统一 Markdown 结构：标题层级、列表符号、代码块、段落空行。
2. 补全结构小节标题，例如把散落的要求整理到「任务」「约束」「输出格式」小节下。
3. 把非标准占位符统一成 `{{变量名}}`，例如 `[项目名称]`、`<项目名称>`、
   `{{ 项目名称 }}` 统一为 `{{项目名称}}`。
4. 修正明显的排版错误：多余空行、错位的编号、Markdown 语法错误。
5. 元数据优化：标题更简洁、分类归入已有分类、标签补齐到 8 个以内、
   适用场景补全。

### 禁止的改动（黑名单）

1. 不得新增原文没有的约束、要求、示例、背景知识。
2. 不得删除或弱化任何一条原始要求。
3. 不得改变提示词的目标、语气、受众和角色设定。
4. 不得改变输出格式的实际含义，只能规范化写法。
5. 不得改变变量语义，不得静默重命名用户已有的变量。
6. 不得添加模型平台专属限制，例如「仅适用于某个模型」。
7. 不得编造事实、数据或专业结论。

### 变量安全规则

- 本期不允许重命名已有变量，AI 最多只能把非标准写法统一成 `{{变量名}}`。
- 优化前后的变量集合必须完全一致。AI 如果新增、删除或改名变量，结果直接判定为
  无效，提示重新生成，不进入保存流程。

### 长度与限额

- 单条正文最多 20000 个字符，与 AI 合并接口保持一致。
- 优化后正文不得超过 30000 个字符，超过时禁止保存并提示用户手动处理。
- 优化后正文超过原文 1.5 倍时，预览中显示提醒。
- 优化后正文比原文减少超过 20% 时，预览中高亮提醒。AI 删内容比加内容更危险。

## 用户交互设计

### 入口

提示词详情抽屉增加「AI 优化」按钮，放在「编辑」按钮旁边。

编辑抽屉本期不增加入口：编辑抽屉里可能有未保存的修改，两套草稿容易互相覆盖。
如果希望在编辑时也能优化，需要「先保存再优化」的确认步骤，建议放到后续版本。

### 优化面板

点击后打开优化抽屉，分区显示：

1. 当前提示词摘要：标题、分类、标签、适用场景。
2. 可选「优化要求」文本框，最多 2000 个字符。示例：

```text
统一成 Markdown 小节
不要改动变量名
保留原有示例
```

3. 「开始优化」按钮，点击前提示「将把这条提示词的完整内容发送给 AI 服务」。

生成过程中显示进度提示；失败时保留用户输入，并提供重试入口。

### 优化结果

生成后显示：

- 优化说明：只读，1 至 6 条，说明改了什么、没改什么。
- 可编辑草稿：标题、分类、标签、适用场景、Markdown 正文。
- 差异对比：正文按行对比，新增行、删除行、修改行分别标识；长文本默认只展示变化
  摘要，可展开查看全文。
- 变量变化清单：写法统一的前后对照。
- 「重新生成」按钮：对结果不满意时可以再生成一版，不会覆盖原始数据。

### 保存

保存按钮必须说明影响，例如：

```text
保存优化结果，并保留优化前版本（30 天内可回退）
```

点击保存前不修改任何数据。保存成功后：

- 提示词更新为优化结果。
- 生成一份「优化前」快照。
- 页面提示「已保存，可回到优化前」，并提供回退入口。

### 回到优化前

保存成功后的提示里带一个「回到优化前」入口，只能回退最近一次优化：

- 回退必须二次确认。
- 回退不是删除版本，而是把当前内容替换为快照内容。
- 回退时同时生成一份「回退前」快照，避免回退本身不可撤销。
- 完整的优化历史列表推迟到后续的统一版本历史功能，本期不做。

## 数据模型

复用 `v0.8.0` 已建的 `prompt_versions` 表，不新增表。

### `version_reason` 取值

| 取值 | 含义 | 来源 |
| --- | --- | --- |
| `merge_before` | AI 合并前的目标提示词快照 | `v0.8.0` |
| `optimize_before` | AI 优化前的提示词快照 | 本期新增 |
| `restore_before` | 回退前的提示词快照 | 本期新增（取决于待确认问题 3） |

`version_reason` 是 `text not null`，没有枚举约束，因此**不需要修改表结构**。

### 快照数量与保留

- 沿用 30 天过期策略，由现有清理任务统一处理，不新增定时任务。
- 本期不实现快照数量上限，30 天后由清理任务删除；个人使用场景下数量可控。

### 本地 SQLite

- `version_reason` 的类型从固定的 `"merge_before"` 放宽为联合类型。
- 其余结构不变，不需要数据迁移，升级过程不得丢失现有数据。

## 接口设计

### AI 优化接口

```text
POST /api/ai/optimize-prompt
```

输入：

```json
{
  "prompt": {
    "id": "prompt-1",
    "title": "标题",
    "category": "分类",
    "tags": ["标签"],
    "content": "正文",
    "useCase": "适用场景"
  },
  "instruction": "可选优化要求"
}
```

输出：

```json
{
  "draft": {
    "title": "优化后的标题",
    "category": "分类",
    "tags": ["标签"],
    "content": "优化后的正文",
    "useCase": "适用场景",
    "optimizationSummary": ["统一了小节结构", "把 [项目名称] 统一为 {{项目名称}}"],
    "variableChanges": [
      { "type": "normalized", "from": "[项目名称]", "to": "{{项目名称}}" }
    ]
  }
}
```

接口规则：

- 云端模式必须先登录。
- 正文最多 20000 个字符，优化要求最多 2000 个字符，超限返回明确错误。
- 超时 50 秒，失败返回明确错误，不写入任何数据。
- 提示词正文作为数据传入，不作为系统指令执行。
- AI 输出必须经过结构化校验，字段超长时按现有规则处理。
- 优化说明至少 1 条、最多 6 条。
- 服务端必须校验优化前后的变量集合一致，不一致时返回明确错误，不返回草稿。
- AI 不得直接写数据库。

### 保存接口

```text
POST /api/prompts/ai-optimize
```

输入只包含优化结果、提示词标识和客户端生成的 `versionId`。快照内容必须由服务端
在事务内读取当前提示词生成，客户端提交的「优化前内容」不得进入快照。这一点与
`v0.8.0` 的合并提交保持一致。

- 云端：新增 PostgreSQL 函数 `commit_prompt_optimize`，使用 `SECURITY INVOKER`，
  在同一事务内完成「校验用户 → 锁定并读取当前提示词 → 写入 `optimize_before`
  快照 → 更新提示词」。
- 本地：使用同一个 SQLite 事务完成等价操作。

### 回退接口

```text
POST /api/prompts/ai-optimize/restore
```

输入为提示词标识。服务端找到这条提示词最近一次未被恢复的 `optimize_before` 快照，
在事务内恢复内容并把它标记为已恢复。云端使用 `restore_prompt_optimize` 函数，
本地使用等价事务。

### 数据源接口扩展

`PromptDataSource` 增加：

```text
commitAiOptimize
fetchLatestOptimizeVersion
restoreOptimizeVersion
```

## 错误处理

- AI 生成失败或超时：保留用户输入，可重试，不修改数据。
- AI 返回格式错误：提示重试，不写入数据库。
- 用户取消：不修改数据。
- 保存失败：事务回滚，提示词保持原样，不出现部分更新。
- 回退失败：快照状态和提示词内容都保持不变。
- 网络失败：提示重试，不自动重放写操作。

## 安全要求

- 所有读写都必须遵守当前用户隔离，云端依赖 RLS，本地为单用户模式。
- 云端只有登录用户可以调用优化接口。
- 不把提示词全文、AI Key、请求头写入应用日志。
- 不向浏览器暴露 AI Key 和 Service Role Key。
- 优化结果必须经过用户确认后才写入数据库。

## 成本影响

- 复用现有 `AI_API_BASE_URL`、`AI_API_KEY`、`AI_MODEL`，不引入新库或新服务。
- 每次优化调用 1 次 AI 请求，输入约等于单条提示词全文，成本低于 AI 合并。
- 不新增定时任务，不新增第三方依赖。

## 测试范围

### 单元测试

- 请求校验：正文长度、优化要求长度。
- 变量集合一致性校验：新增、删除或改名变量都要被判为无效。
- AI 输出解析和异常 JSON 处理。
- 优化说明与变量变化清单清洗。
- 行级差异对比计算。
- `version_reason` 类型放宽后的兼容处理。

### 数据库测试

- 保存优化结果时生成 `optimize_before` 快照。
- 保存具有原子性，失败时整体回滚。
- 回退恢复为快照内容并标记 `restored_at`。
- 过期清理覆盖优化快照。
- 本地 SQLite 旧库升级不丢失数据。

### 云端验证

- 未登录不能调用优化接口。
- 用户只能读取自己的优化记录。
- 优化后刷新页面结果一致。
- 回退后内容正确，来源数据不受影响。

## 验收标准

- [ ] 详情抽屉显示「AI 优化」入口。
- [ ] 只有用户点击后才会发出 AI 请求。
- [ ] 优化结果包含只读的优化说明。
- [ ] 保存前可以编辑全部字段。
- [ ] 差异对比可以看出新增、删除和修改。
- [ ] 变量变化清单展示写法统一的前后对照。
- [ ] 变量集合在优化前后不一致时，直接判为无效并提示重新生成。
- [ ] 用户取消、AI 失败、格式错误都不修改数据。
- [ ] 保存后提示词更新，并生成优化前快照。
- [ ] 保存操作具有原子性。
- [ ] 30 天内可以一键回到优化前（最近一次）。
- [ ] 优化结果不满意时可以重新生成。
- [ ] 优化后正文明显变短或变长时，预览中有提醒。
- [ ] 本地与云端行为一致。
- [ ] 不同账号之间的数据完全隔离。
- [ ] `npm run check` 全部通过。

## 风险与处理

### AI 改动超出「不改变原意」

- 在 system 提示词中写明白名单和黑名单。
- 强制返回优化说明，超出范围的改动要在预览中可见。
- 保存前必须人工确认。

### 变量被改名导致已填过的变量值失效

- 默认禁止重命名。
- 只有用户打开开关才允许，并展示完整映射表。

### 正文被越改越长

- 超过 30000 个字符禁止保存。
- 超过原文 1.5 倍时显示提醒。

### 预览信息过载

- 差异对比默认折叠为摘要，可展开查看全文。
- 优化说明最多 6 条。

### 快照过多

- 沿用 30 天过期策略，并限制单个提示词的优化快照数量。

## 已定方案

前 5 项在 2026-09-20 的复盘里定稿，第 6、7 项是 2026-09-21 的范围收敛结论。
实施时按此执行；如果后续发现不合适，可以单独提出修改，不阻塞其他部分。

1. **正文改动采用「整段替换 + 全文对比 + 可编辑草稿」**：与 AI 合并的交互一致，
   我的学习成本最低；「逐段建议、逐条接受」会把复杂度推到前后端段落映射上，
   收益不明显。
2. **本期完全禁止重命名变量**（2026-09-21 修订）：原方案打算留一个开关，但开关会让
   AI 行为和测试矩阵翻倍，收益很小。确需改名时可以手工编辑。
3. **回退时生成新的「回退前」快照**：回退本身也是一次覆盖。没有新快照就无法撤销
   误操作，这与「AI 不得静默覆盖内容」一致。
4. **不设快照数量上限，只保留 30 天过期策略**（2026-09-21 修订）：个人使用场景下数量
   可控，省掉一套计数和删除逻辑。
5. **版本号为 `v0.9.0`**。
6. **优化历史列表本期不做**（2026-09-21 补充）：v0.8.0 已经有「合并恢复记录」，两个列表
   放在不同入口属于重复。等做统一的版本历史时再合并展示，数据现在就会照常存下来。
7. **补充四项保护**（2026-09-21 补充）：变量集合硬校验、正文缩水提醒、重新生成按钮、
   发送前告知全文会上传给 AI 服务。前三项针对「AI 悄悄改坏内容」这个最大风险。

## 任务顺序

一个任务一次提交，按顺序做。表里的文件是预计改动范围，实施时以实际需要为准。

| 序号 | 任务 | 主要改动文件 |
| --- | --- | --- |
| 1 | 快照原因类型放宽到 `optimize_before` / `restore_before` | `src/lib/server/prompt-database.ts` |
| 2 | AI 优化接口、输出校验和变量集合一致性校验 | `src/app/api/ai/optimize-prompt/route.ts`、`src/lib/prompt-ai.ts` |
| 3 | 云端提交与回退函数 | `supabase/migrations/` 新增一个迁移文件 |
| 4 | 数据源方法：提交优化、读取最近一次优化快照、回退 | `src/lib/prompt-source.ts`、`src/lib/server/prompt-database.ts` |
| 5 | 优化抽屉：入口、草稿编辑、差异对比、变量清单、重新生成、回到优化前 | `src/components/prompt-optimize-drawer.tsx`、`src/components/prompt-detail-drawer.tsx` |
| 6 | 自动化测试与人工验收记录 | `tests/`、`docs/acceptance/` |

## 当前阻塞

本功能依赖 `v0.8.0` 提供的 `prompt_versions` 快照表和清理任务，因此顺序是：

```text
v0.8.0 合并到 main
-> 云端执行 Supabase 迁移并验证
-> 再开始 v0.9.0
```

注意：`main` 分支推送会触发 Vercel 生产部署，所以在 Supabase 迁移执行并验证之前，
`v0.8.0` 分支不能先合并到 `main`，否则线上代码会依赖尚不存在的数据库结构。
