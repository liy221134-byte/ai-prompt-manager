# v2.14.0 验收清单（沉淀回流）

## 怎么用

- 版本：`v2.14.0`，主题是「把项目里攒的东西升到公共库，并跟踪两边有没有分叉」。
- 状态：**待产品负责人验收**。
- 对应设计：[v2.14.0 沉淀回流设计](../superpowers/specs/2026-09-24-v2.14.0-sediment-flowback-design.md)；
  H1–H3 已按建议确认（提升时项目那份不动、只做公共→项目一向拉取、体检放公共资产的更多里）。

## 验收记录

- 2026-09-24 由 Codex 开发。
- 自动化检查：`npm run test` 474 项全通过（新增 6 项：提升计划、上游状态、沉淀体检）；
  `npm run lint` 0 错误；`npm run build` 通过；本机页面无编译问题。

## 验收标准

- [x] 项目里任意规则／文档／模板／图谱节点能「提升为公共资产」，公共库出现同名副本，
      两条都带「同源」关系。依据：`tests/sediment-flowback.test.mjs`。
- [x] 提示词提升时给出原因（它本来就是账号级），不重复提升已提升过的条目。
      依据：同一条测试。
- [x] 公共库那份更新后，打开项目里这条会看到「公共库那份在这之后改过」的提示与两个按钮。
      依据：同一条测试的「上游状态」部分。
- [x] 「看差异」能看正文行级差异（复用提示词优化的差异视图）。
      依据：`src/components/sediment-diff-dialog.tsx` 复用 `diffPromptLines`。
- [x] 「拉取公共版本」产生一条新版本，旧正文可从版本记录恢复。
      依据：走 `buildUpdateAssetInput`（与手动保存同一条路径）。
- [x] 沉淀体检在公共资产 → 更多里，三类线索都能列出来，且不自动做任何动作。
      依据：`tests/sediment-flowback.test.mjs`「沉淀体检」。
- [x] 全流程不删除任何数据；本机与云端共用同一套纯逻辑与契约测试。
      依据：同一条测试 + 契约测试。

## 需要线上确认的

- [ ] 在线上把一条项目规则提升为公共资产，再改公共那份，回项目里看提示与差异。
- [ ] 打开沉淀体检，确认清单符合预期。

## 设计偏差

（如开发与设计不一致，在这里逐条列出。）

## 未完成事项与风险

- 不做自动同步、不做双向引用、不做删除（设计里明确不做）。
- 沉淀体检的「标题重复」只按标题精确匹配，不做相似度；内容级去重留到以后。
