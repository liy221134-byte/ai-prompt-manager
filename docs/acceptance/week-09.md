# 第 9 周验收清单（v0.9.0 提示词 AI 优化）

## 版本与结论

- 目标版本：`v0.9.0`
- 分支：`feat/v0.9.0-ai-prompt-optimize`
- 本地与代码自动化验证：通过
- 远程数据库函数：已执行并验证（迁移文件随合并自动应用）
- 人工验收：待我在浏览器里逐条确认

## 已通过本地与代码验证

- [x] 快照原因类型放宽到 `optimize_before` / `restore_before`
- [x] 合并恢复记录只列出合并快照，优化快照不会混入
- [x] 合并恢复通道不能消费优化快照
- [x] AI 优化接口 `/api/ai/optimize-prompt` 只在用户点击后调用
- [x] 系统提示词包含白名单、黑名单和变量规则
- [x] 默认状态下变量集合不一致会判为无效并提示重新生成
- [x] 非标准占位符（如 `[行业]`）统一成 `{{行业}}` 不需要额外放行
- [x] 勾选「允许调整变量」后，增删改名会在预览中列出并要求二次确认
- [x] 优化说明只读展示，1 至 6 条
- [x] 保存优化结果会先生成优化前快照，再更新提示词（原子事务）
- [x] 保存失败时整体回滚，不出现部分更新
- [x] 回到优化前会恢复内容，并留下回退前快照
- [x] 没有可回退记录时返回明确提示，不写入任何快照
- [x] 行级差异对比可以区分新增、删除和相同行
- [x] 正文比原文减少超过 20% 时界面会提醒
- [x] 本地 SQLite 与云端 Supabase 行为一致
- [x] 未登录不能调用 AI 优化和保存接口
- [x] `npm run check` 已通过：lint、typecheck、119 项测试和 build

## 人工验收步骤

1. 打开任意提示词详情，点右上角的星标按钮，确认打开「AI 优化」抽屉。
2. 直接点「开始优化」，确认生成优化说明、正文差异和变量变化。
3. 确认不勾选「允许调整变量」时，AI 一旦增删变量会被拦下并提示重新生成。
4. 勾选后再生成，确认变量变化被列出来并要求二次确认。
5. 保存后确认提示「优化结果已保存，可以回到优化前」。
6. 关掉再打开这条提示词，确认详情里出现「可以回到优化前」。
7. 点「回到优化前」并确认，检查标题、正文和标签都回到优化前的状态。
8. 确认回退之后「可以回到优化前」入口消失。
9. 云端模式重复第 1 至 8 步。

## 已知限制

- 界面只支持回到最近一次优化前，不支持选择更早的版本。
- 回退后留下的「回退前」快照暂时没有界面入口，只作为数据层兜底保留。
- 优化抽屉在编辑抽屉里没有入口，避免和未保存的修改互相覆盖。
- 优化记录保留 30 天，到期由清理任务删除。

## 发布顺序

1. 合并到 `main` 并推送，Supabase 自动应用 `202609210001_add_prompt_optimize.sql`。
2. Vercel 构建通过后在生产环境跑一遍人工验收。
3. 确认无误后打标签 `v0.9.0`。
