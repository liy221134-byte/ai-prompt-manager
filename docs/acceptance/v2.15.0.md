# v2.15.0 验收清单（立项起步包）

## 怎么用

- 版本：`v2.15.0`，主题是「立项时不抓瞎」：该装什么有推荐、该产出什么有提示词、
  交付有整包、编译产物不再越攒越多。
- 状态：**待产品负责人验收**。
- 对应设计：[v2.15.0 立项起步包设计](../superpowers/specs/2026-09-24-v2.15.0-kickoff-pack-design.md)。

## 验收记录

- 2026-09-24 由 Codex 开发。
- 自动化检查：`npm run test` 478 项全通过（新增 4 项：推荐、立项提示词、交付包）；
  `npm run lint` 0 错误；`npm run build` 通过。

## 验收标准

- [x] 工程基线里能看到「建议从公共库装这些」清单，每条有推荐理由；
      点「把这 N 条装进项目」后，装进去的条目从清单里消失。
      依据：`tests/project-kickoff.test.mjs`「推荐：硬约束规则优先，已经装过的不再推荐」。
- [x] 推荐里**不推公共库的实例文档**，只推规则、模板和方法级参考。
      依据：同文件「推荐：模板会带上，公共库的实例文档不会往项目里推」。
- [x] 「立项与交付」面板里有四条提示词（PRD／Spec／技术档案／验收清单），
      每条能复制，内容包含项目名和项目说明。
      依据：同文件「立项提示词：四份产物都在」。
- [x] 能下载开发体系包，文件按规则／文档／模板分节，只有当前项目的内容。
      依据：同文件「交付包：按规则、文档、模板分节，文件名带项目名」。
- [x] 「存成项目里的一份文档」后，项目文档里出现这份包（同名时更新，不重复建）。
      依据：`handleSaveDeliveryPack`（走与保存文档同一条路径）。
- [x] 同一个项目第二次编译同一产物走**更新**，不新增同名文档，旧内容在版本记录里。
      依据：`handleSaveCompiledDraft`（同名 + 角色「编译结果」时走更新）。

## 需要线上确认的

- [ ] 线上打开工程基线，看推荐清单是否符合直觉，装一两条试试。
- [ ] 线上用「立项与交付」复制一条 PRD 提示词，贴给 AI 看产出是否够用。
- [ ] 线上导出一次开发体系包，检查分节和内容。

## 设计偏差

1. 设计里写「编译产物改成更新这一份」，实现时确认此前已经按这个语义实现（同名 + 角色
   「编译结果」走更新），本版只补了验收；没有重复改动。

## 未完成事项与风险

- 推荐只看等级关注的规则方向做关键词匹配，不做语义判断；装不装由人决定。
- 交付包是单文件 Markdown，不带 ZIP、不带格式版本号。
