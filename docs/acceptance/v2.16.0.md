# v2.16.0 验收清单（MCP 批量入库与沉淀分流建议）

## 怎么用

- 版本：`v2.16.0`，把断点清单里最后两条单独立项的做完。
- 状态：**待产品负责人验收**（MCP 相关条目只在本机可验）。

## 验收记录

- 2026-09-24 由 Codex 开发。
- 自动化检查：`npm run test` 487 项全通过（新增 9 项：MCP 两个批量工具的功能用例、
  分流建议 5 条）；`npm run lint` 0 错误；`npm run build` 通过。

## 验收标准

### MCP 批量入库

- [x] `import_documents` 能把一个本机目录里的 Markdown 按批导成项目文档，原样入库。
      依据：`tests/mcp-server.test.mjs`「按目录批量灌文档」。
- [x] 先加 `dryRun` 只列清单、不写库；真导之后重复调用会跳过同名文档并说明原因。
      依据：同一条测试（第二次调用断言「可导入 0 份，跳过 2 份」+ 原因）。
- [x] `import_graph_nodes` 按编号批量建节点，同类型同编号跳过、不覆盖正文；
      节点说明必填。
      依据：同文件「批量灌节点：同类型同编号跳过，其余按编号新建」。
- [x] `MCP_READ_ONLY=1` 时这两个工具不注册（只读客户端看不到）。
      依据：同文件「只读模式只注册查询工具」。

### 沉淀自动分流建议

- [x] 沉淀体检里每条"只在项目里的规则"都带「建议升公共／建议留项目」和理由。
      依据：`tests/sediment-scope.test.mjs`（通用约束→升、点名项目/版本/路径→留）。
- [x] 有「把建议升公共的 N 条一次提升」按钮；提升仍走人工确认的那条路径。
      依据：`sediment-checkup-drawer.tsx` + `handlePromoteAssetsToPublic`。
- [x] 看不出通用性的默认建议留项目，并写明理由。
      依据：同一条测试「看不出通用性的先留在项目里」。

## 需要线上／本机确认的

- [ ] 本机用 MCP 客户端跑一次 `import_documents`（先 dryRun），确认清单符合预期。
- [ ] 打开沉淀体检，看分流建议是否符合你的直觉，点一次批量提升。

## 设计偏差

（如开发与设计不一致，在这里逐条列出。）

## 未完成事项与风险

- 分流建议是关键词匹配，不做语义判断；宁可默认"留项目"，也不建议乱升。
- MCP 的目录导入读的是本机路径，云端不适用（MCP 本来只在本机）。
