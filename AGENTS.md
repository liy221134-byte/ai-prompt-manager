# 项目规则

这份文件是本项目唯一的规则来源。技能说明、模板文档或口头约定与它冲突时，以本文件为准。

## 关于我

我是产品经理，不直接阅读代码。所有结论用中文说明，用变更清单和验收步骤让我理解结果。

## 每次开始工作前

1. 先读 `docs/INDEX.md`，再按任务读取相关产品和工程文档。
2. 开新项目或新版本时，先按 `docs/project-map.md` 判定规模、运行环境和阶段，再决定这次要读哪些文档、产出什么、过哪道门禁。
3. 动手前先用自然语言说明：准备改什么、为什么改、我会看到什么变化、可能影响哪些文件或功能。

## 开发流程：一个版本、一份文档、三道门

### 需求门

- 需求模糊、涉及新方向或存在多种做法时，先对话确认，不写代码。
- 需求清晰时直接进入设计门。

### 设计门

- 触发条件：满足任意一条时，先写一份《设计 + 任务清单》文档，等我确认后再开发。
  1. 会留下长期痕迹：数据表、字段、迁移、恢复或回滚语义。
  2. 会改变对外约定：AI 接口输入输出、优化与合并规则、鉴权与数据隔离。
  3. 会引入外部依赖、外部服务或持续成本。
- 不触发：界面、文案、样式、局部实现调整，以及已有测试覆盖的重构。这些只需在动手
  前用自然语言说明改什么、为什么改、影响哪些文件、怎么验收。
- 文档只写决定，不写实现过程，只保留四块内容：
  1. 做什么 / 不做什么。
  2. 数据和产品行为影响。
  3. 验收标准，每条都能用一次测试或一次手工验证判定。
  4. 任务顺序，以及每个任务改哪些文件。
- 正确性由验收保证，不由文档自己声明：
  - 不写自检章节和覆盖率对照表，这类自己证明自己的内容无效。
  - 交付时逐条对照验收标准；实现和设计不一致时，单独列出「设计偏差」，由我决定改
    设计还是改实现。
- 只写决定，不写实现步骤、完整 SQL 和成段代码，那些属于实施过程。
- 篇幅以「把事情说明白」为准，在此基础上尽量精简。允许为了说清楚而写长，不允许为了
  写短而留下含糊表述：没说清楚的地方，后续实现会自行脑补，比文档长更危险。

### 交付门

1. 先写或更新测试，再写实现；验证失败时先用测试复现问题。
2. 运行 `npm run check`（代码检查、类型检查、自动化测试、生产构建），把真实结果告
   诉我，不能只说“应该没问题”。
3. 做一轮独立自查，发现的问题直接修进当前任务的提交里，不额外制造 fix 提交。
4. 交付说明包含：改了哪些文件、每个文件改了什么、如何预览或验证、未完成事项和风险。

## 技能使用规则

### 默认使用

- 需求模糊时：使用 `brainstorming`。
- 涉及逻辑、数据或接口时：使用 `test-driven-development`。
- 交付前：使用 `verification-before-completion`。

### 默认不使用

- `writing-plans`、`executing-plans`：由《设计 + 任务清单》替代。
- `subagent-driven-development` 和并行子代理：个人项目默认不启用，除非我明确要求。
- `using-git-worktrees`：默认在项目目录内建功能分支，不额外建工作区，避免检查命令
  跑错目录。
- `requesting-code-review`、`receiving-code-review`：合并为交付门里的那一轮自查。
- `finishing-a-development-branch`：只在整个版本合并时使用。
- `using-superpowers`：不再作为每次对话的固定开场。

### 使用边界

- 只读取本次真正要用到的技能说明，不批量加载技能文件。
- 技能文档与本文件冲突时，按本文件执行，并在交付说明里指出冲突点。

## 版本与提交

- 一个版本只做一个子系统；需要同时改数据和界面、或涉及多条链路时，拆成两次发布。
- 一次提交只处理一个清晰目标，提交信息使用 `feat:`、`fix:`、`docs:`、`test:`、
  `chore:` 前缀。
- 功能开发前确认当前 Git 状态，`main` 分支始终保持可运行。
- 每个阶段完成后记录提交和版本标签。
- 未经确认，不执行 `git reset --hard`、强制推送或删除 Git 历史。

## 产品规则

1. 个人使用的 AI 提示词资产管理工具；使用个人邮箱密码登录保护云端数据，不做团队
   协作和多租户权限。
2. 提示词卡片包含标题、分类、标签、正文和适用场景。
3. 核心使用流程保持顺滑：找到提示词 -> 填写变量 -> 一键复制 -> 使用。
4. 采集流程保持简单：粘贴原文 -> AI 识别结构和标签 -> 我确认 -> 保存。
5. AI 不得静默覆盖我的内容；AI 生成、优化、合并的结果必须先预览，由我确认后再保存。
6. 变量识别和复制行为按 `docs/acceptance/week-02.md` 的验收标准执行。

## 数据与安全

1. 未经明确确认，不删除文件、不删除或覆盖数据、不执行破坏性 Git 操作。
2. 未经确认，不引入第三方库、外部服务、远程仓库或部署环境。
3. 只有我主动点击后才把内容发送到 AI 服务；提示词全文不写入日志，密钥不进入浏览器
   代码和 Git。
4. 优先修改指定范围，不做无关重构；只有我明确要求时才重写整个文件。
5. 普通实现细节可以自行决定，但要在交付说明里解释。

## 代码与文案

1. 重要代码块、复杂逻辑和容易误解的地方写中文注释，不给每一行代码加注释。
2. 所有用户可见文案使用中文。
3. 不提交无法运行的代码，不提交带 `TODO` 的占位功能。
