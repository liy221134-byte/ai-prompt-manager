# 项目规则

这份文件是本项目唯一的规则来源。技能说明、模板文档或口头约定与它冲突时，以本文件为准。

## 关于我

我是产品经理，不直接阅读代码。所有结论用中文说明，用变更清单和验收步骤让我理解结果。

## 每次开始工作前

1. 先读 `docs/INDEX.md`，再按任务读取相关产品和工程文档。
2. 立项、开新版本、规模变化、阶段跳跃、交付移交时，按 `docs/project-map.md` 判定规模、
   运行环境和阶段，并把它编译成项目自己的规则文档（`AGENTS.md`、验收清单、运维手册）。
   日常开发读这些产物，不再回头读地图。
3. 涉及代码改动时，先读 `docs/engineering-map.md` 定位要改的文件，再精读那几个文件，
   不要整篇读大文件。
4. 动手前先用自然语言说明：准备改什么、为什么改、我会看到什么变化、可能影响哪些文件或功能。

## 开发流程：一个版本、一份文档、三道门

### 需求门

- 需求模糊、涉及新方向或存在多种做法时，先对话确认，不写代码。
- 需求清晰时直接进入设计门。

### 设计门

- 触发条件：满足任意一条时，先写一份《设计 + 任务清单》文档，等我确认后再开发。
  1. 会留下长期痕迹：数据表、字段、迁移、恢复或回滚语义。
  2. 会改变对外约定：AI 接口输入输出、优化与合并规则、鉴权与数据隔离。
  3. 会引入外部依赖、外部服务或持续成本。
- 不触发：界面、文案、样式、局部实现调整，以及已有测试覆盖的重构。这些只需在动手
  前用自然语言说明改什么、为什么改、影响哪些文件、怎么验收。
- 文档只写决定，不写实现过程，只保留四块内容：
  1. 做什么 / 不做什么。
  2. 数据和产品行为影响。
  3. 验收标准，每条都能用一次测试或一次手工验证判定。
  4. 任务顺序，以及每个任务改哪些文件。
- 正确性由验收保证，不由文档自己声明：
  - 不写自检章节和覆盖率对照表，这类自己证明自己的内容无效。
  - 交付时逐条对照验收标准；实现和设计不一致时，单独列出「设计偏差」，由我决定改
    设计还是改实现。
- 只写决定，不写实现步骤、完整 SQL 和成段代码，那些属于实施过程。
- 篇幅以「把事情说明白」为准，在此基础上尽量精简。允许为了说清楚而写长，不允许为了
  写短而留下含糊表述：没说清楚的地方，后续实现会自行脑补，比文档长更危险。

### 交付门

1. 先写或更新测试，再写实现；验证失败时先用测试复现问题。
2. 按档位运行检查，把真实结果告诉我，不能只说“应该没问题”：
   - 日常任务：`npm run check:fast`（类型检查 + 自动化测试，约 40 秒）。
   - 合并或推送前：`npm run check`（再增加代码检查和生产构建）。
   - 纯文档、纯内容改动可以只跑测试，但要说明实际跑了什么。
3. 做一轮独立自查，发现的问题直接修进当前任务的提交里，不额外制造 fix 提交。
4. 交付说明包含：改了哪些文件、每个文件改了什么、如何预览或验证、未完成事项和风险。
5. 证据只记一处：同一次发现只写进验收清单或决策记录中的一处，其他文档放链接引用，
   不重复叙述。

## 写代码的规矩（防过度设计）

AI 默认会多写：加抽象、装依赖、顺手重构。动手写代码前，按顺序停在第一个成立的台阶上：

1. 这件事需要做吗？不需要就不做。
2. 项目里已经有能复用的实现吗？有就复用，不重写。
3. 标准库、框架自带的能做吗？能就用。
4. 平台原生能力能做吗（例如浏览器自带的日期选择）？能用就用。
5. 已经装过的依赖能做吗？能做就用。
6. 能一行写完吗？一行写完。
7. 以上都不成立，才写满足需求的最小实现。

梯子是在看懂问题和整条链路之后才爬，不能用「简化」的名义跳过理解。

- 不加没人要的抽象、脚手架和依赖。
- 修 bug 按四步走：
  **先复现**（写清操作步骤、数据、报错原文；复现不了就先加观测，不改业务逻辑）
  → **定位根因**（先搜这个函数的全部调用方，改共享函数一次，不只补报告里提到的那条路径；
  结论要能解释"为什么之前会那样"）→ **最小修复**（只改根因那一处，顺手重构另开一次）
  → **回归**（补一条能挡住同类问题的检查，再按交付门跑一次检查）。
- 简化不许砍掉：校验、错误处理、安全、无障碍、防数据丢失和真实边界条件。
- 有意留下的简化，要在代码里注明它的上限和以后怎么升级。
- 非平凡逻辑留下一个能运行的检查，口径与交付门的「先写测试」一致；一行小改不强制写测试。
- 做完主动说明「这次省掉了什么」，不默默省。

两条边界优先于上面的规矩：

1. 设计门不变。涉及数据表、对外接口、新依赖时，仍然先写设计文档等你确认，「先做最简版再问」不适用。
2. 删除规矩不变。删文件、删数据、删功能仍要先得到你的确认；「简化优先」只限本次任务自己新增或直接改动的代码。

## 技能使用规则

### 默认使用

- 需求模糊时：使用 `brainstorming`。
- 涉及逻辑、数据或接口时：使用 `test-driven-development`。
- 交付前：使用 `verification-before-completion`。

### 默认不使用

- `writing-plans`、`executing-plans`：由《设计 + 任务清单》替代。
- `subagent-driven-development` 和并行子代理：个人项目默认不启用，除非我明确要求。
- `using-git-worktrees`：串行开发默认在项目目录内建功能分支；多个会话并行时必须用独立
  工作区隔离，否则会出现提交落到别人分支上的情况。
- `requesting-code-review`、`receiving-code-review`：合并为交付门里的那一轮自查。
- `finishing-a-development-branch`：只在整个版本合并时使用。
- `using-superpowers`：不再作为每次对话的固定开场。

### 使用边界

- 只读取本次真正要用到的技能说明，不批量加载技能文件。
- 技能文档与本文件冲突时，按本文件执行，并在交付说明里指出冲突点。

## 版本与提交

- 一个版本只做一个子系统；需要同时改数据和界面、或涉及多条链路时，拆成两次发布。
- 一次提交只处理一个清晰目标，提交信息使用 `feat:`、`fix:`、`docs:`、`test:`、
  `chore:` 前缀。
- 每次提交前确认当前分支和工作区状态。工作目录可能被其他会话切走，提交落到错误分支
  是实际发生过的错误。
- 开新版本前，先确认上一个版本的验收清单已经签完（见 `docs/acceptance/`）。有未签项
  就先补验收，不开新功能。
- 多个会话并行开发时用独立工作区隔离（`git worktree` 或第二个目录）；只有串行开发
  才共用同一个目录。
- `main` 分支始终保持可运行，每个阶段完成后记录提交和版本标签。
- 未经确认，不执行 `git reset --hard`、强制推送或删除 Git 历史。

## 产品规则

1. 个人使用的 AI 提示词资产管理工具；使用个人邮箱密码登录保护云端数据，不做团队
   协作和多租户权限。
2. 提示词卡片包含标题、分类、标签、正文和适用场景。
3. 核心使用流程保持顺滑：找到提示词 -> 填写变量 -> 一键复制 -> 使用。
4. 采集流程保持简单：粘贴原文 -> AI 识别结构和标签 -> 我确认 -> 保存。
5. AI 不得静默覆盖我的内容；AI 生成、优化、合并的结果必须先预览，由我确认后再保存。
6. 变量识别和复制行为按 `docs/acceptance/week-02.md` 的验收标准执行。

## 数据与安全

1. 未经明确确认，不删除文件、不删除或覆盖数据、不改动生产库、不执行破坏性 Git 操作。
   生产库（Supabase 云端）的写入先说清改什么、为什么、怎么回滚，我同意后再做，做完登记
   一条记录到 `docs/operations/database-migrations.md` 的「事实记录」里。
   同意一次一议，只在对话里给，不存在「以后这类都可以」。
2. 未经确认，不引入第三方库、外部服务、远程仓库或部署环境。
3. 只有我主动点击后才把内容发送到 AI 服务；提示词全文不写入日志，密钥不进入浏览器
   代码和 Git。
4. 优先修改指定范围，不做无关重构；只有我明确要求时才重写整个文件。
5. 普通实现细节可以自行决定，但要在交付说明里解释。

## 代码与文案

1. 重要代码块、复杂逻辑和容易误解的地方写中文注释，不给每一行代码加注释。
2. 所有用户可见文案使用中文。
3. 不提交无法运行的代码，不提交带 `TODO` 的占位功能。
