# 项目规则

## 关于我

我是产品经理，不直接阅读代码。请通过中文说明、变更清单和验收步骤让我理解结果。

## 工作原则

开始工作前，先阅读 `docs/INDEX.md`，再按任务读取相关产品和工程文档。

1. 开始修改前，先用自然语言说明准备改什么、为什么改、修改后的用户可见效果，以及可能影响的文件或功能。
2. 重要代码块、复杂逻辑和容易误解的地方写中文注释，不要给每一行代码都加注释。
3. 未经明确确认，不删除文件，不删除或覆盖用户数据，不执行破坏性 Git 操作。
4. 未经确认，不引入第三方库或外部服务。
5. 优先修改指定范围，不进行无关重构。只有明确要求重构时，才可以重写整个文件。
6. 遇到会影响数据结构、产品行为、外部依赖或存在明显成本差异的决定时，先询问我。
7. 普通实现细节可以自行决定，但必须在交付说明中解释。

## 产品规则

1. 当前产品是个人使用的 AI 提示词资产管理工具。
2. 当前使用个人邮箱密码登录保护云端数据，但不做团队协作和多租户权限。
3. 提示词卡片从第一版起包含标题、分类、标签、正文和适用场景。
4. 核心使用流程必须保持顺滑：找到提示词、填写变量、一键复制、使用。
5. 采集流程必须保持简单：粘贴原文、AI 识别结构和标签、用户确认、保存。
6. AI 不得静默覆盖用户内容。所有 AI 生成或合并结果都必须先预览并由用户确认。

## 交付规则

1. 每次完成后列出修改了哪些文件、每个文件改了什么、如何预览或验证，以及未完成事项。
2. 不提交带 `TODO` 的产品占位功能。
3. 所有用户可见文案使用中文。
4. 不提交无法运行的代码。修改后至少完成一次基本验证。
5. 所有功能变更都要有明确验收条件。
6. 未经确认，不创建远程仓库、外部服务或部署环境。

## Git 规则

1. 功能开发前确认当前 Git 状态。
2. `main` 分支始终保持可运行。
3. 一次提交只处理一个清晰目标。
4. 提交信息使用 `feat:`、`fix:`、`docs:`、`test:`、`chore:` 等前缀。
5. 每个阶段完成后记录提交和版本标签。
6. 未经确认，不执行 `git reset --hard`、强制推送或删除 Git 历史。
