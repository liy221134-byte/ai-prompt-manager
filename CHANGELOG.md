# 变更记录

## v2.14.0

这一版解决「项目里攒的东西升不到公共库、两边改了互相不知道」。

- **提升为公共资产**：项目里的规则／文档／模板／图谱节点，详情右上角一键提升——公共资产库
  多一份副本，项目里那份**保持不动**；两边各记一条「同源」关系，以后互相找得到。
  提示词不参与（它本来就是账号级的）。
- **上游更新提示**：公共库那份改过之后，打开项目里这条，详情顶部会出现提示
  「公共库那份在这之后改过」，给「看差异」和「拉取公共版本」两个动作。**不自动覆盖**。
- **看差异**：复用正文行级差异视图，一眼看清公共库那份多了什么、少了什么。
- **拉取公共版本**：把公共那份的标题、摘要和正文带过来，产生一条新版本；旧正文留在
  版本记录里可以恢复。
- **沉淀体检**（公共资产 → 更多 → 沉淀体检）：只读列出三条线索——
  只在项目里出现、还没提升过的规则；和公共库标题重复的项目资产；公共库里超过 90 天
  没更新的资产。每条给「提升为公共」或「打开」入口，不做任何自动动作。
- 已知边界：没有自动同步、没有双向引用、不做删除；只加一张不需要的前提——全部落在
  现有资产与关系字段上，没有表结构变更。

## v2.13.0

这一版解决「不知道怎么用」和「应该能关联、不该手工重贴」，与 `v2.12.0` **一起上线**
（v2.12.0 未单独发布，改动都在这一个部署里）。

- **文档与模板边界**：公共资产的「文档」页签改名**「参考文档」**，只放方法级参考
  （画像、案例、方法说明）；项目里的文档才是"这次做的事"。同时把 5 条
  `documentType=模板` 的资产**归位成真正的模板**（本机 + 云端，见
  `docs/operations/database-migrations.md` 事实记录）——这就是"文档和模板像在维护同一批内容"
  的根源。
- **补上 Spec 这一层**：新增 `templates/engineering/implementation-spec.md`
  （小功能档 / 大版本档，大版本含数据结构与迁移、对老数据影响、验收映射、风险回滚），
  文档类型新增「实现规格」。根据一次真实讨论：PRD 讲做什么，Spec 讲这次具体怎么改，
  你不读代码时，Spec 是唯一能在代码产生前拦住 AI 的闸门。
- **项目文档按链路分组**：项目视图的文档页签分成**需求 → 规格与计划 → 交付 → 验收与发布**
  四段，每段显示已有几份，空的那段写明"这一格放什么"。
- **工程基线**：新增**链路完整性检查**（缺 PRD／Spec／验收／发布记录时给入口）；
  缺某类文档时，可以直接把项目里已有的文档**标记成那个类型**，不用再新建一份重贴。
- **项目图谱支持批量挂文档**：选一份文档 + 勾选节点，一次给每个节点加一条「引用」关系，
  已挂过的跳过。挂完节点详情的「指向谁／谁指向它／间接影响」三层就有内容了。
- **验收记录关联录入**：编辑器里写清"先选对应需求节点，再挂相关文档"；
  链路检查里也能直接建验收记录（自动带出还没通过验收的需求）。
- **导入工程新增「项目文档」来源**：选一批 Markdown **原样**建成文档资产（标题取文件名、
  正文就是原文、不走 AI、同名默认不导入、单份 ≤2 MB、一次 ≤100 份）。
- **技术档案可以不用手填**：项目设置里能**复制一段技术档案提示词**交给 AI，
  再把结果贴回编辑器（零代码、没有 `package.json` 的项目走这条）。
- **本机 MCP 自检**：`npm run mcp:check` 一条命令拉起来服务、列项目、数图谱节点、做一次影响分析。
- **数据搬运写进文档**：本地与云端之间怎么搬（导出→导入，只新增不覆盖）、
  代跑脚本 `npm run sync:from-cloud` 的用法，见 README 与 `docs/operations/backup.md`。
- 已知边界：移动端未专门适配；本地专属能力仍只有两项（分析代码目录、MCP）。

## v2.12.0

这一版来自 2026-09-24 的线上自测反馈：把挤在一页的东西分开、把点不动的浮窗修好、
把新项目从哪拿资产说清楚。一次交付覆盖了原计划的两批（交互重构 + 资产来源）。

- **修 bug**：规则包详情、规则编译、项目图谱、导入工程、工程基线这 5 个抽屉只写了内层
  `absolute` 定位、缺一层固定覆盖，页面一滚动浮层就跑到可视区外、按钮点不到。
  现在统一成与其余 15 个抽屉一致的写法；新增结构测试挡住同类问题再犯。
- **首屏拆成两个视图**：顶部切「公共资产」和「项目」。公共资产是账号级方法库
  （提示词、规则、文档、模板、规则包），原来的「默认项目」就是这个视图；项目视图放
  这个项目自己的规则、文档、模板、图谱节点、验收记录、发布记录。刷新后停在原视图。
- **主按钮与筛选收敛**：类型标签从 10 个变成每个视图 6／7 个，主按钮从 15 个变成
  每个视图 2–4 个 + 一个「更多」菜单；状态、标签、关系目标、规则包收进「筛选」，
  搜索框回到主位，生效筛选带角标和一键清空。
- **技术档案归位**：它是项目属性、一个项目只有一份，从类型标签移进「项目设置」，
  并且能选一个本机 `package.json` 推断技术栈草稿（草稿必须人工确认后才保存）。
- **新项目有资产来源**：项目视图空状态给三条来源（从公共资产库挑、装规则包、导入工程），
  「从公共资产库挑资产」按来源去重、只新增不覆盖，两边各自独立。
- **规则包答疑落地**：导入面板写清规则包包含什么，并提供「下载示例规则包」；
  把数据备份文件当规则包导入时，提示改成「这是数据备份文件，要恢复数据请用
  数据管理 → 导入备份」；新增「打包成规则包」（勾选规则／文档生成包文件，
  只生成文件、不改库里资产）和[规则包格式说明](docs/rule-pack-format.md)。
- **提示词不再显示资产关系**：提示词是一次性取用的东西，关系对它没有实际用处；
  数据保留，只是不显示（`showPromptRelations` 改回 true 即可恢复），关系集中在项目资产。
- **文档**：新增[用户故事与线上自测清单](docs/user-stories.md)（39 条，原 US-13 拆成
  「公共资产视图」和「项目视图」两条，并补 4 条新能力）。
- **第二轮自测修复**：规则编译的假勾选框改成按条「排除这条」按钮（原来点一下像全选）；
  示例规则包字段修正，并且装载前先按产品自己的资产结构校验，字段不对就当场说明是哪一条、
  哪个字段；读云端资产时跳过读不出来的那一行，不再让一条脏数据拖挂整个列表；
  「导入文档包」补齐云端链路（原文进私有桶、确认用登录会话写库），原来云端直接报
  「云端模式不支持本机数据接口」。
- **本机模式更好启动**：新增 `npm run dev:local`，需要「分析代码目录」和 MCP 时用这一条命令。
- 已知边界：移动端未专门适配（主要用 PC）；不带数据表／字段／迁移改动，所以不需要
  对云端库做任何结构变更。

## v2.11.0

这一版不新增功能，是把 2.0 这整条线统一验一遍（对应计划里的「加固与验收」）。

- **迁移与结构**：整条迁移链在真实 Postgres 上跑通并可重复执行；老本机库打开时自动补列（质量等级）；云端只读复核——41 条资产、项目带质量等级、来源包私有桶正常、每日心跳任务在跑。
- **备份与恢复**：v2 备份全类型往返演练通过——1 个项目 + 82 条资产（提示词／规则／文档／模板／图谱节点／验收记录／发布记录 7 类）+ 2 条关系，导入空库后逐项一致，标识与标题不变；v1 旧备份仍可导入。
- **回滚**：实测跨版本回滚的风险并写进手册——库里只要有旧版本不认识的资产类型（例如用 v2.9.0 的代码去读含「发布记录」的库），**旧代码启动就失败**；回滚前要先导出备份、再删掉旧版本不认识的那几类资产。同时把两条读路径的报错从「资产数据无法识别」改成带类型和处置建议。同一版本内回滚没有这个问题。
- **安全边界**：匿名 key 直读云端库返回 401；配置缺失时安全关闭不降级；未登录访问受保护页面跳登录；来源包 20 MB／单次导入 200 条／代码扫描 3000 文件与单文件 400 KB／12 层／编号唯一等限额都有测试覆盖；AI 改不了验收结论、也写不了发布记录。
- 修复：备份说明补上「每个资产的历史版本不在备份里」（原来只写了来源包原文，而恢复后每个资产只有 1 条版本记录）。
- 修复：手工复制本机库文件时只复制 `.sqlite` 会丢掉 WAL 里的最近写入（演练里这样复制出来的库少了 1 条刚建的记录）；备份文档写明要么先正常关掉应用、要么三个文件一起复制。
- 收尾：清理 `%TEMP%` 下 15 项演练产物和演练用的 git worktree；README、v2-plan、INDEX、pending-items 与备份/回滚两份运维文档同步。
- 已知边界：备份不含历史版本和来源包原文；跨「引入新资产类型」的版本回滚要先清理数据（没有改成静默跳过，少显示数据比报错更危险）；线上域名自测仍单独挂待办。

## v2.10.0

- 新增资产类型「发布记录」：一次上线留一条，记录**版本号、发布日期、结果（进行中／已发布／已回滚）、回滚目标和门禁清单**。门禁每一项能勾「做完了」并写一句证据说明，例如「工程检查通过」「备份已生成并在隔离环境恢复验证过」「迁移在隔离环境跑通过」。标题按版本号自动生成（例如「v2.10.0 发布记录」），不用另想名字。
- 门禁清单按项目质量等级自动带出：个人工具 2 项、低风险生产 5 项、涉及用户数据 6 项、高敏感 7 项；可以自己增删改。
- 工程基线新增「最近一次发布」：显示版本、结果、门禁完成情况（x／y）和还没完成的那几项，带「打开记录」和「新建发布记录」。
- AI 不能自己发证：写工具的允许类型里没有发布记录，调用会被参数校验拒绝；发布记录只能人工填——门禁是人工可验证证据。
- 新增故障演练记录模板（`templates/engineering/drill-record.md`）：演练目标、隔离环境、步骤与结果、发现的问题、后续动作；第一段写明红线——绝不在生产代码和唯一生产数据上演练。工程文档模板到九份。
- 已知边界：不接 CI（门禁人工勾选）；不做演练记录的结构化类型；不做发布审批流、多环境编排和自动回滚；加固与验收统一放到 v2.11.0。

## v2.9.0

- 新增资产类型「验收记录」：正文写验收条件、测试步骤、实际结果和已知问题；元数据存**对应需求节点、结论、提交版本和证据清单**（说明 + 链接或文件路径）。这样一条链就齐了：需求编号 → 验收条件 → 测试步骤 → 结果与证据 → 提交版本。
- 结论有四种：待确认／通过／未通过／例外。新建和 AI 提交的记录一律先是「待确认」，**改成「通过」只能由人在界面上点**——MCP 的写入字段里根本没有「结论」这一项，从接口层面堵住，不靠提示词。
- 工程基线新增「验收覆盖」：列出还没通过验收的需求（未通过记录多的排前面），每条旁边就是「新建验收记录」，点进去标题和对应需求节点已经填好；需求节点详情里也会显示「验收记录：1 条，通过 1 条」。
- 「例外」算未覆盖：只有结论是「通过」才算这条需求验收过；草稿和归档的验收记录不算数。
- 类型筛选、搜索、备份和 MCP 查询都覆盖了这种新类型（MCP 能按需求编号提交记录、填证据和提交版本）。
- 修复：详情抽屉以前把非规则资产一律叫「文档」、正文字段叫「选型说明」，验收记录显示成了「文档 / 选型说明」；现在按类型显示「验收记录 / 验收条件、步骤与实际结果」。
- 已知边界：不接 GitHub／CI（提交号手填）；不做证据附件上传；不做验收条件自动生成；不做跨项目统计看板；发布门禁记录排在 v2.10.0。

## v2.8.0

- 新增项目**质量等级**：个人工具／低风险生产／涉及用户数据／高敏感项目。项目设置和新建项目里都能选；等级决定这个项目该有哪些工程文档、该关注哪些规则方向、发布前要做哪些检查。老项目自动按「个人工具」读，不用回头改数据。
- 新增「工程基线」：工具栏进入，按等级列出三份清单——**必须有的工程文档**（对照项目里现有资产算出「已有／还缺」，缺失项点一下直接进新建文档，标题和文档类型已经填好）、**建议关注的规则方向**、**发布前必须完成的检查**。只做对照和跳转，不自动创建任何东西。
- 八份工程文档模板进仓库（`templates/engineering/`）：架构与请求链路、数据流、环境变量清单、发布与回滚、备份与恢复、安全检查、故障处理手册、成本与性能基线。用现有的「导入模板」装进项目，带内容门禁测试（文件清单、一级标题、项目名占位符、维护约定）。
- 文档类型新增七种：架构说明、数据流说明、环境变量清单、备份说明、安全检查、故障手册、成本与性能（发布与回滚复用已有的「发布手册」）。
- 数据库：项目表加 `risk_level` 一列，本机库缺列时自动补列、老项目读成「个人工具」；云端迁移文件可重复执行并带取值约束，已随 GitHub 集成应用到线上。
- 修复：导入文档包时创建项目没有带质量等级，本机写库会失败。
- 修复：云端读项目改成不写死列名，迁移晚于代码上线时也不会整站读不出项目。
- 已知边界：不记录「这次发布检查做没做」（发布门禁与演练记录排在 v2.10.0）；不做验收证据链（v2.9.0）；不自动创建文档、不自动装规则包；不做第三方安全扫描和成本采集。

## v2.7.0

- 新增本机 MCP 服务：`npm run mcp` 起一个标准输入输出服务，AI 客户端按 MCP 配置连过来，就能直接查库、也能把结果写回来。不开端口、不需要账号和密钥，客户端在哪个目录启动都能找到同一个本机库。
- **六个查询工具**：列项目（含资产条数）、搜提示词（按项目／关键词／标签，返回正文）、取资产（正文、元数据、指向谁、被谁指向）、列规则（可按作用层级／类型／范围筛，带理由和来源片段）、列图谱节点（编号、完整路径、备注）、算影响分析（指向谁／谁指向它／间接影响，间接那条写明通过谁）。
- **三个写入工具**：新建资产（提示词／规则／文档／图谱节点／模板，只新增不覆盖）、编辑资产（只覆盖传了的字段，没传的保持原样）、改状态（草稿／待确认／活跃／已废弃／已归档）。每次写入都产生一条不可变版本，旧版本留在历史里可以从界面恢复；写入的资产来源标成「AI」，界面上一眼能分辨。
- 边界：**没有任何删除类工具**——不进垃圾箱、不做永久删除、不做清空垃圾箱；技术档案和项目不开放写入；关系仍然只能在界面上建。配 `MCP_READ_ONLY=1` 时只注册查询工具。
- 新增依赖 `@modelcontextprotocol/sdk` 和 `zod`，都放在开发依赖里，只被本地脚本使用，线上产物不变。
- 已知边界：只有本机能用（不做远程 MCP、不做鉴权和多用户）；云端 Supabase 库不参与；写入没有「先预览、我确认」这一步，靠版本历史和「没有删除入口」兜底；加固与验收等整个 2.0 产品做完后统一做。

## v2.6.0

- 新增「导入工程」：工具栏进入，两个来源，都是先出草稿、勾选确认之后才写资产。
  - 导入数据库 Schema：粘贴建表语句或选 `.sql` 文件，解析成「数据」节点——编号 `TBL-表名`，正文是字段清单（类型、非空、默认值、主键），备注写明来源；外键在同批或库里已有同编号节点时自动连成「依赖」关系（增量导入下一份迁移时也能接上）。
  - 分析代码目录：填本机目录路径，扫出「模块」节点（一层目录一个，按目录层级挂父子）和「接口」节点（API 路由，正文列支持的 HTTP 方法，按路径前缀挂父子）。依赖目录、构建产物、所有点开头的目录都跳过；单文件超过 400 KB 只进清单不读内容，一次最多 3000 个文件，符号链接不跟。云端模式读不到本机目录，这一栏会禁用并写明原因。
- 导入的共同规则：同项目同类型里编号已存在的默认标成「已存在」并且不勾选，导入只补新节点、不覆盖你已经改过的正文；导入的节点来源标成「导入」，备注写清来自哪个文件或目录。
- 修复：外键指向上一批已经导入过的表时关系会断在解析层（批内找不到就直接丢了），现在按 `TBL-表名` 记编号，落库时库里有同编号节点就连上。
- 修复：导入面板切「Schema／代码目录」页签时，React 会把「选择 SQL 文件」的输入框复用成路径输入框，触发受控/非受控告警。
- 已知边界：不解析 `ALTER TABLE`、视图、函数和触发器，只认 `CREATE TABLE`；代码目录分析只在本机模式可用；不分析 `import`／`require` 调用关系；不做增量差异界面；MCP 动态查询单独一版；加固与验收等整个 2.0 产品做完后统一做。

## v2.5.0

- 新增图谱节点资产：资产类型开放「图谱节点」，分需求／模块／数据／接口／测试五类，各带稳定编号（例如 `REQ-001`）、父节点和备注。同一项目同一类型里编号不能重复，重复保存会被拦下并提示「编号「REQ-001」已经被「需求一：资产入库」用了，换一个。」；父节点只列同类型的节点。资产标识和编号分开，改编号不影响已有引用。
- 新增「项目图谱」视图：工具栏进入，顶部按五类切换并显示每类数量，左边是树（父子缩进、按编号排序），右边看选中节点的编号、完整路径、说明和备注，外加三块清单——「这个节点指向谁」「谁指向它」「间接影响」。归档和垃圾箱里的节点不出现；父节点被归档或删掉时，子节点按根显示，不会从树里消失。
- 影响分析：直接一层加上间接一层，间接那条写清是通过谁串起来的（实测 REQ-001 显示入向 1 条、间接 1 条，标注「通过 需求一必须保留原文」）。清单里每条都能点开对应的资产或节点，也能一键跳到节点编辑器补关系。只读现有关系字段，不自动判定严重程度，不做全图遍历。
- 已知边界：不自动导入数据库 Schema、不分析代码模块、不自动猜映射（留给 Agent 连接那一版）；不做图形化的数据流视图和追踪矩阵；节点类型固定五类，不支持自定义；不做批量建节点和拖拽排序；加固与验收等整个 2.0 产品做完后统一做。

## v2.4.0

- 模板变成资产：预留的「模板」类型正式开放，元数据记产物文件名和备注；变量不单独存，从正文里的 `{{变量}}` 实时解析，详情里能看到变量清单；类型筛选新增「模板」。
- 新增「导入模板」：一次选多个 Markdown，每个文件直接建成一条模板资产（不走 AI，原文即模板正文），文件名作为产物文件名。仓库里的 `templates/new-project/` 四个文件已按这个方式导入验证。
- 规则编译支持套模板：编译面板可选产物模板，主产物按模板结构生成——项目名称、项目说明、技术栈自动填（变量名写法不同也能按关键词识别），`{{规则集}}` 位置插入规则段落，模板里没写这个占位符时把规则追加到末尾并提示；剩下来没填的变量在面板上列出来。`START_PROMPT.md` 保持内置结构。
- 套模板生成的产物同样能复制、下载、保存成文档资产（文件名取模板的产物文件名）。
- 已知边界：模板不做变量类型校验和必填拦截；不做模板市场与共享；模板只参与规则编译这一条生成流程；加固与验收等整个 2.0 产品做完后统一做。

## v2.3.0

- 新增规则编译：工具栏「规则编译」把项目里该编译的规则挑出来，按作用层级和规则类型分组；不是活跃状态、标了「不参与编译」和已被裁决排除的规则单独列出并写明原因。
- 冲突候选与人工裁决：把「一条必须、一条禁止，并且至少在一个维度上相关（同层级／同范围／技术上下文有交集）」的规则对摆出来，写明重叠在哪；可以一键排除其中一条，也可以恢复参与。裁决记在规则元数据里（参与／排除、备注、时间），下次打开还是这个结论。
- 生成 `AGENTS.md` 和 `START_PROMPT.md` 草稿：按层级分组、组内按规则类型排，每条带陈述、理由、来源包、可信度和验证方式；头部写明项目名、生成时间、参与条数和排除条数，有技术档案时带上一行技术栈摘要。`START_PROMPT.md` 只收「必须」和「禁止」。
- 草稿可以复制、下载，也能保存成文档资产（角色自动标「编译结果」），保存后跳到该资产详情，版本记录正常。
- 修复：编辑器保存会抹掉改不到的元数据（可信度、编译去向、所属包、编译裁决）——编辑一次导入的规则，「来自哪个包」就没了。
- 已知边界：冲突判定只看结构信号，不做语义比对和自动裁决；每次保存编译产物都会新建一条文档资产，不做覆盖更新；模板中心、图谱和 MCP 不在本版；加固与验收等整个 2.0 产品做完后统一做。

## v2.2.0

- 新增规则包：可复用的规则集合，带包版本、可信度、适用项目规模和成员清单。一个项目可以装多个包，安装按「项目 + 包 + 包内编号」去重，不覆盖你改过的内容；同一个包能装进多个项目，成员标识自动加序号、包内引用关系跟着重映射。
- 新增「导入规则包」：选一个包文件就能看到包名、版本、可信度、适用规模和成员构成，再选项目一键安装；「导出这个包」把库里的包导出成可搬运的文件，重复成员只导一次。
- 列表新增「规则包」类型筛选和按包筛选；选中包时自动切到「全部」标签（提示词不属于任何包）。
- 规则新增「理由」和「来源片段」两个字段，编辑器和详情都能填能看；详情多一个「可信度」标签。
- 种子资产包正式进产品：新增 `npm run pack:seed`，把仓库里的种子包编译成规则包文件（1 个包 + 27 条成员：12 条规则、15 条文档），并有「产物与源文件一致」的漂移测试；种子包资产的「失败模式」小节会落到规则的「理由」。
- 修复：技术档案的编辑入口拿不到资产（第二次点「技术档案」会打开空编辑器并报错）。
- 修复：种子包里没有 front-matter 编号的附属文档缺包内编号，安装时被整批跳过（实测少 7 份验证记录）。
- 修复：安装计数把同一个包内编号算多次（同一个包装到多个项目后）。
- 修复：按包筛选时停在「提示词」标签会看到空列表，现在自动切到「全部」。
- 已知边界：规则冲突检测与编译产出 `AGENTS.md`／`START_PROMPT.md` 放到下一版；规则包不做在线市场与共享；备份导入仍然没有预览步骤。

## v2.1.2

- 新增技术档案：一个项目一份，记录技术栈清单；选型偏离默认时，必须挂一条 ADR 文档说明原因，否则保存会被拦下。
- 新增资产关系：引用、依赖、替代、实现四类，单向可查。提示词、规则、文档、技术档案都能建关系，详情里既看得到「这条指向谁」，也看得到「被谁引用」。
- 列表升级：类型筛选新增「技术档案」；类型、状态、标签、关系目标四个条件可以叠加；搜索范围扩展到技术栈名称和关系说明。
- 备份升级到 v2：项目、提示词、规则、文档、技术档案和它们之间的关系一起导出；旧版提示词备份仍可导入。导入只新增不覆盖，同名项目按名称合并到已有项目。来源包上传的原文件不在备份里，需要另行保留。
- 修复：关系目标下拉会把正在编辑的资产自己也列进去，可能建出「指向自己」的关系；现在提示词和资产两套编辑器都会排除自己。
- 修复：保存、删除、恢复、合并、优化、导入提示词之后，资产列表没有跟着刷新，导致新建的提示词不能马上当关系目标、资产详情的「被谁引用」显示旧标题；现在一并刷新。
- 修复：提示词详情里，指向已归档资产或垃圾箱提示词的关系没有「目标已归档」标注；现在会标注。
- 修复：备份弹层的说明还写着「只包含提示词，规则和文档不在备份范围内」，改成 v2 的实际范围。
- 已知边界：备份导入没有预览步骤（已确认放到后续版本）；关系目标被删除或归档后，筛选条件不会自动复位；资产之间的关系联动与相互转换留到后续版本。

## v2.1.1

- 新增文档包导入：上传 Markdown、纯文本和 ZIP，原文完整保留——本地放在数据目录的来源目录，云端放在 Supabase 私有桶，资产里只记相对路径，二进制不进资产正文。
- AI 只产出草稿：生成「项目 + 资产清单」草稿，可以在预览里改类型、合并、按小标题拆分、勾掉不创建的条目，确认之后才创建项目和资产。
- AI 提取的规则默认是草稿状态，不会自动进入活跃列表；每个资产第 1 版的来源指向对应的来源包。
- 规则元数据补齐用途、技术上下文、作用层级、执行阶段、优先级、生命周期、覆盖权限、来源证据和验证方式；文档元数据补齐文档角色（来源、工作稿、权威版、编译结果）、是否权威来源、所属模块、生效版本、来源位置、更新触发条件、新鲜度和最后验证时间。核心字段必填，扩展字段可留空，旧数据缺字段按空值读出来。
- 自己实现了 ZIP 读取器（压缩与不压缩条目、条目数与体积上限、拒绝 64 位包），没有引入第三方解压库。
- 修复：切换到非默认项目时，如果停在「提示词」标签会看到空列表、而其他类型的计数又不是 0，容易误以为数据丢了；现在会自动切回「全部」。
- 已知边界：导入的规则统一先落成「建议 + 项目范围」的草稿，具体类型由用户在编辑器里改；文档包里的图片等非文本文件只会列进「没认出来的文件」。

## v2.1.0

- 提示词写入路径切到统一资产：本地和云端的提示词读写、垃圾箱、AI 合并、AI 优化都走资产表和资产函数，旧提示词表保留为只读快照。
- 云端新增 7 个原子操作函数（进垃圾箱、合并提交、恢复合并、优化提交、回到优化前、清空垃圾箱、取版本号），不改表结构，也不删除旧函数，回滚到 2.0.0 的代码仍然可用。
- 修复云端资产迁移在真实 Postgres 上执行失败的两处错误：回填版本记录时引用了只有本地才有的 `source_prompt_ids_json` 列名，以及回填默认项目时裸 `null` 被推断成 `text`。
- 新增在真实 Postgres 上跑整条迁移链的测试，以后改迁移会随 `npm test` 一起验证回填、重复执行、行级安全和资产函数。
- 清掉重构后遗留的未使用代码，lint 恢复零警告。
- 已知边界：备份目前只覆盖提示词；回滚到旧代码前必须先导出备份，否则切换之后写入的数据在旧代码里看不到。

## v2.0.0

- 首屏从单一提示词库升级为项目资产库，提示词、规则和文档纳入统一资产模型。
- 新增项目工作区：新建、重命名、归档、重新激活和项目切换，刷新后记住最近使用的项目。
- 资产列表支持按项目、类型和状态筛选，默认只显示活跃资产。
- 新增规则和文档的新增、编辑、查看和归档，规则带规则类型和适用范围，文档带文档类型。
- 资产版本不可变：每次正式保存产生一个版本，恢复到历史版本会生成新版本，不覆盖旧记录。
- 现有提示词数据迁移到默认项目，提示词标识、合并、优化和垃圾箱恢复关系保持不变。
- 旧版提示词备份继续可以导入，导入的提示词进入默认项目。
- 修复「回到优化前」失败：回退前快照编号改为服务端生成，不再复用已消费的编号。
- 修复多个弹窗同时打开时页面滚动锁互相干扰的问题。
- 已知边界：备份目前只覆盖提示词，规则和文档不在备份范围内；云端 Supabase 迁移尚未应用。

## v1.0.0

- 完成个人 AI 提示词资产管理工具第一版整体收尾。
- 新增变量管理：按变量逐条改名、降级为正文，支持选区设为变量和光标处插入变量。
- 保存提示词前会校验变量空名、重名、花括号、名称长度和变量数量上限。
- AI 采集后进入编辑器时，可以直接确认和整理识别出的变量。
- 详情抽屉增加「整理变量」入口，编辑和采集共用同一套变量规则。
- 变量仍然从正文派生，不新增数据库字段，不需要数据库迁移。
- 修复详情抽屉与编辑抽屉同时打开时的重复渲染警告。

## v0.9.1

- 变量管理设计落地到采集、编辑和详情流程。
- 变量改名会同步更新正文中的全部出现位置。
- 降级变量会去掉占位符语法，但保留变量文字。
- 补充变量编辑纯函数和自动化测试。

## v0.9.0

- 支持对单条提示词做 AI 优化：整理元数据和正文结构，不改变原意。
- 系统提示词写明白名单和黑名单，禁止新增、删除或弱化原始要求。
- 优化结果包含只读的优化说明，逐条说明改了什么、没改什么。
- 优化前后提供行级差异对比，长文本默认只显示变化摘要，可展开看全文。
- 默认不允许改变变量集合；AI 增删或改名变量会被拦下并提示重新生成。
- 非标准占位符（例如 `[行业]`）统一成 `{{行业}}` 属于白名单动作，不需要额外放行。
- 勾选「允许调整变量」后，AI 可以删除牵强变量、改名或补充变量，预览会列出全部变化并要求二次确认。
- 正文比原文减少超过 20% 时，界面会提醒确认有没有丢内容。
- 对结果不满意时可以重新生成一版，不会覆盖原始数据。
- 保存优化结果先生成优化前快照，再更新提示词，整个过程是原子事务。
- 保存后 30 天内可以一键回到优化前，回退本身也会留下回退前快照。
- 合并恢复记录只列出合并快照，优化快照不会混入。
- 本地 SQLite 与云端 Supabase 行为一致。
- 界面只支持回到最近一次优化前，完整版本历史后续再做。

## v0.8.0

- 支持选择 2 至 5 条提示词进行 AI 合并。
- 第一条选中项默认为目标，保存前可调整，并支持可选合并要求。
- AI 返回可编辑草稿和合并说明，用户确认后才写入数据。
- 合并保存为原子事务，目标更新、来源归档和恢复快照任一步失败会整体回滚。
- 空来源数组会在写入快照和目标前被拒绝并回滚。
- 合并前目标内容保存为恢复快照，其他来源进入垃圾箱。
- 恢复快照内容由服务端读取锁定后的目标行生成，客户端只提供版本标识。
- 垃圾箱支持恢复、永久删除和清空，默认保留 30 天。
- 云端清空垃圾箱通过单个 `SECURITY INVOKER` RPC 原子执行。
- 包含本地启动清理与云端每日清理代码路径；云端执行待远程验证。
- 本地 SQLite 与 Supabase 使用相同的生命周期字段和恢复语义。
- 备份只导出内容字段，导入时不会恢复已在垃圾箱中的提示词。
- 备份导入预览与实际导入统一跳过当前在垃圾箱中的提示词。
- 不提供完整版本历史界面，仅保留合并恢复快照。
- 远程 Supabase 迁移与顾问检查已于 2026-09-20 执行并通过。

## v0.7.0

- 增加独立登录页 `/login` 和服务端登录保护。
- 未登录访问受保护页面时返回登录页，登录后返回原本访问的页面。
- 使用 Supabase `getClaims()` 校验会话，并统一保护页面和 API。
- 未登录请求受保护 API 返回 `401`。
- 云端模式拒绝本机 SQLite 数据接口。
- 站外登录返回地址会被安全过滤。
- 登录页初始化成功或失败都会结束等待状态。
- Vercel 配置错误时安全关闭，不降级到本机 SQLite。
- Vercel 构建强制执行完整工程检查。
- Node.js 版本固定为 `24.x`。
- 完成第 7 周 Preview 登录、刷新、退出和 API 保护验收。

## v0.6.0

- 完成 DeepSeek AI 智能采集。
- AI 自动生成标题、分类、标签、Markdown 正文和适用场景。
- AI 结果进入编辑器，用户确认后才保存。
- 增加 AI 服务端代理、登录保护和结构化结果校验。

## v0.5.0

- 部署到 Vercel Hobby。
- 使用 Supabase PostgreSQL、邮箱密码登录和 RLS。
- 增加数据库心跳和云端环境变量配置。

## v0.4.0

- 使用本机 SQLite 建立共享数据库。
- 本机多浏览器共享提示词。
- 增加旧 `localStorage` 数据迁移。

## v0.3.0

- 增加 JSON 导出。
- 增加导入预览、冲突处理和合并。
- 增加最近备份时间。

## v0.2.0

- 增加提示词新增、编辑、删除和搜索。
- 增加变量识别、填写、实时预览和复制。
- 增加本地持久化。

## v0.1.0

- 建立 Next.js、TypeScript 和 Tailwind 项目。
- 完成静态提示词卡片页面。
